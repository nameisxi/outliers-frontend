import { Descriptions, Badge, Tag, Divider, Typography, Row, Col, Statistic } from 'antd';

const { Title } = Typography;


function ContactDetails(props) {
    return (
        <div>
            <Title level={3}>Contact</Title>
            <br/>
        </div>
    );
}

export default ContactDetails;
